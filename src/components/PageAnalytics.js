"use client";import { useEffect } from "react";import { usePathname } from "next/navigation";
export default function PageAnalytics(){const path=usePathname();useEffect(()=>{let consent;try{consent=JSON.parse(localStorage.getItem("propwealth-cookie-consent")||"{}").value}catch{}if(consent!=="all")return;let sessionId=sessionStorage.getItem("propwealth-session"),visitorId=localStorage.getItem("propwealth-visitor");if(!sessionId){sessionId=crypto.randomUUID();sessionStorage.setItem("propwealth-session",sessionId)}if(!visitorId){visitorId=crypto.randomUUID();localStorage.setItem("propwealth-visitor",visitorId)}const width=window.innerWidth,device=width<768?"mobile":width<1100?"tablet":"desktop",ua=navigator.userAgent,browser=/Edg/.test(ua)?"Edge":/Chrome/.test(ua)?"Chrome":/Safari/.test(ua)?"Safari":/Firefox/.test(ua)?"Firefox":"Other",operatingSystem=/Windows/.test(ua)?"Windows":/Mac/.test(ua)?"macOS":/Android/.test(ua)?"Android":/iPhone|iPad/.test(ua)?"iOS":"Other";fetch("/api/analytics",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({path,title:document.title,referrer:document.referrer,sessionId,visitorId,device,browser,operatingSystem,language:navigator.language,timezone:Intl.DateTimeFormat().resolvedOptions().timeZone}),keepalive:true})},[path]);return null}
