import Link from "next/link";import PageHero from "./PageHero";import { CheckCircle2 } from "lucide-react";
export default function AboutTopicPage({eyebrow,title,intro,heading,body,points=[],children}){return <><PageHero eyebrow={eyebrow} title={title}>{intro}</PageHero><section className="section"><div className="shell about-topic"><div><span className="eyebrow">PropWealth approach</span><h2>{heading}</h2>{body.map(paragraph=><p key={paragraph}>{paragraph}</p>)}{points.length>0&&<ul className="check-list">{points.map(point=><li key={point}><CheckCircle2/>{point}</li>)}</ul>}</div>{children}</div><div className="shell topic-back"><Link href="/about">← Back to About Us</Link></div></section></>}
