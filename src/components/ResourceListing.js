import Image from "next/image";import Link from "next/link";import PageHero from "./PageHero";import { getResourceEntries } from "../lib/resourceEntries";
export default async function ResourceListing({type,eyebrow,title,description,basePath}){const items=await getResourceEntries(type);return <><PageHero eyebrow={eyebrow} title={title}>{description}</PageHero><section className="section"><div className="shell">{items.length?<div className="resource-public-grid">{items.map(item=><article className="resource-public-card" key={item.slug}>{item.coverImage&&<Link href={`${basePath}/${item.slug}`} className="resource-public-cover"><Image src={item.coverImage} alt={item.title} fill sizes="(max-width:800px) 100vw, 33vw"/></Link>}<div><span className="chip">{item.category||title}</span><h2><Link href={`${basePath}/${item.slug}`}>{item.title}</Link></h2><p>{item.excerpt}</p><footer>{item.duration&&<span>{item.duration}</span>}<Link href={`${basePath}/${item.slug}`}>View resource →</Link></footer></div></article>)}</div>:<div className="empty-state"><h3>No published content yet</h3><p>New {title.toLowerCase()} will appear here when published from the dashboard.</p></div>}</div></section></>}
