import Image from "next/image";import Link from "next/link";import { notFound } from "next/navigation";import { getAuthor,getPublishedBlogs } from "../../../../lib/blogs";
export const dynamic="force-dynamic";export default async function AuthorPage({params}){const {slug}=await params;const author=await getAuthor(slug);if(!author)notFound();const posts=await getPublishedBlogs({author:author._id});return <><section className="page-hero"><div className="shell author-profile">{author.avatar&&<Image src={author.avatar} alt={author.name} width={130} height={130}/>}<span className="eyebrow">PropWealth author</span><h1>{author.name}</h1><strong>{author.title}</strong><p>{author.bio}</p></div></section><section className="section"><div className="shell blog-grid">{posts.map(post=><article className="blog-card" key={post.slug}>{post.coverImage&&<Link className="blog-cover" href={`/blog/${post.slug}`}><Image src={post.coverImage} alt={post.title} fill/></Link>}<div><span className="chip">{post.category}</span><h2><Link href={`/blog/${post.slug}`}>{post.title}</Link></h2><p>{post.excerpt}</p></div></article>)}</div></section></>}
