import { readFile,stat } from "node:fs/promises";import { safeUploadPath } from "../../../../lib/uploads";
const types={jpg:"image/jpeg",jpeg:"image/jpeg",png:"image/png",webp:"image/webp",gif:"image/gif",avif:"image/avif",svg:"image/svg+xml",mp4:"video/mp4",webm:"video/webm",mov:"video/quicktime",m4v:"video/x-m4v",avi:"video/x-msvideo"};
export async function GET(request,{params}){try{const {filename}=await params;const file=safeUploadPath([decodeURIComponent(filename)]);const info=await stat(file);const range=request.headers.get("range");const ext=filename.split(".").pop().toLowerCase();const headers={"Content-Type":types[ext]||"application/octet-stream","Cache-Control":"public, max-age=31536000, immutable","Accept-Ranges":"bytes"};if(range){const [startText,endText]=range.replace("bytes=","").split("-");const start=Number(startText);const end=endText?Number(endText):info.size-1;const buffer=await readFile(file);return new Response(buffer.subarray(start,end+1),{status:206,headers:{...headers,"Content-Range":`bytes ${start}-${end}/${info.size}`,"Content-Length":String(end-start+1)}})}return new Response(await readFile(file),{headers:{...headers,"Content-Length":String(info.size)}})}catch{return new Response("Media not found",{status:404})}}
