import crypto from "node:crypto";import { connectDB } from "../../../lib/mongodb";import PageView from "../../../models/PageView";
export async function POST(request){try{const consent=request.cookies.get?.("propwealth_consent")?.value||request.headers.get("cookie")?.match(/propwealth_consent=([^;]+)/)?.[1];if(consent!=="all")return Response.json({ok:false},{status:202});const body=await request.json();if(!body.path?.startsWith("/"))return Response.json({error:"Invalid path"},{status:400});await connectDB();const ip=request.headers.get("x-forwarded-for")?.split(",")[0]||"",clean=(value,max=100)=>String(value||"").slice(0,max);await PageView.create({path:clean(body.path,300),title:clean(body.title,200),referrer:clean(body.referrer,500),visitorId:clean(body.visitorId),sessionId:clean(body.sessionId),device:clean(body.device,20),browser:clean(body.browser,40),operatingSystem:clean(body.operatingSystem,40),language:clean(body.language,20),timezone:clean(body.timezone,80),userAgent:clean(request.headers.get("user-agent"),500),ipHash:crypto.createHash("sha256").update(ip+(process.env.JWT_SECRET||"")).digest("hex")});return Response.json({ok:true})}catch{return Response.json({ok:false},{status:202})}}
