import nodemailer from "nodemailer";
export async function sendLeadEmails(lead){if(!process.env.SMTP_HOST||!process.env.SMTP_USER||!process.env.SMTP_PASS)return false;const transport=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:Number(process.env.SMTP_PORT)===465,auth:{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}});const from=process.env.EMAIL_FROM||process.env.SMTP_USER;const team=process.env.EMAIL_TO||process.env.SUPPORT_EMAIL;const cc=[process.env.MEETING_CC_1,process.env.MEETING_CC_2,process.env.MEETING_CC_3].filter(Boolean);await Promise.all([transport.sendMail({from,to:team,cc,replyTo:lead.email,subject:`New property enquiry: ${lead.propertyTitle}`,text:`Name: ${lead.name}\nEmail: ${lead.email}\nPhone: ${lead.phone}\nProperty: ${lead.propertyTitle}\nBudget: ${lead.budget||"Not provided"}\nTimeframe: ${lead.timeframe||"Not provided"}\nExperience: ${lead.experience||"Not provided"}\nGoal: ${lead.goal||"Not provided"}\nPreferred time: ${lead.appointmentDate||"Not provided"}\n\n${lead.message||""}`}),transport.sendMail({from,to:lead.email,subject:`We received your PropWealth enquiry`,text:`Hi ${lead.name},\n\nThanks for your interest in ${lead.propertyTitle}. Your consultation request is safely with our team and we will contact you shortly.\n\nPropWealth\nTrust · Invest · Grow`})]);return true}
